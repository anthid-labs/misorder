[package]
name = "redis-worker-demo"
description = "Two workers taking one lock the naive way, as the system under test in the Redis worked example."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
rust-version.workspace = true
# Not published. This is the thing the example runs misorder against, and it is
# wrong on purpose.
publish = false

[lints]
workspace = true

[[bin]]
name = "redis-worker-demo"
path = "src/main.rs"

[dependencies]
tokio = { workspace = true }
